# SuperNatural Motorsports - Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# SUPABASE (Required for production)
# =============================================================================
# Get these from your Supabase project: Settings > API
# 
# IMPORTANT: Vite requires VITE_ prefix for client-side variables.
# If using Vercel's Supabase integration, copy the values:
#   VITE_SUPABASE_URL = (value from SUPABASE_URL)
#   VITE_SUPABASE_ANON_KEY = (value from SUPABASE_ANON_KEY)

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# =============================================================================
# VERCEL BLOB STORAGE (Optional, for images)
# =============================================================================
# Get this from Vercel Dashboard: Storage > Blob > your-store
# The public URL is used client-side to construct image URLs

VITE_VERCEL_BLOB_URL=https://your-blob-store.public.blob.vercel-storage.com

# This token is used server-side/locally for uploading images
# NEVER expose this to the client
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...

# =============================================================================
# GOOGLE AI (Optional, for image generation)
# =============================================================================
# Used only for local image generation script
# NEVER expose this to the client

GOOGLE_AI_API_KEY=your-google-ai-api-key

# =============================================================================
# NOTES
# =============================================================================
# 
# Variables prefixed with VITE_ are exposed to the browser - only use for
# public values like the Supabase anon key and public blob URL.
# 
# Sensitive keys (service role, blob write token, AI key) should only be
# used in scripts or server-side functions, never in client code.
